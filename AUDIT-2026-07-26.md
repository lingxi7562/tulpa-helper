# Tulpa Helper 功能审计 — 2026-07-26

> 审计维度：实用性 / 便利性 / 可靠性 / 直觉性 / 符合 tulpa 构建原理
> 严重度：🔴 必须修复 · 🟡 建议修改 · 🔵 仅供参考

---

## 研究基线（tulpa 构建原理判据）

来自 tulpa.info 社区指南、r/Tulpas、以及相关临床文献的共识要点，作为「符合构建原理」维度的评判依据：

| # | 原理 | 来源强度 |
|---|------|---------|
| P1 | **Passive forcing / narration 价值极高**，应贯穿日常，而非只在专注时段 | 社区共识强 |
| P2 | **刚性特质列表有争议**：过度规格化会抑制自然涌现，社区倾向「基础轮廓 + 允许演化」 | 社区共识中等，无实证 |
| P3 | **Deviation（偏离预期）是健康且预期内的现象**，不是失败 | 社区共识强 |
| P4 | **怀疑（doubt）是首要障碍**，形成负反馈循环抑制自发信号；建议「先假定是 tulpa」 | 社区共识强 |
| P5 | **过度担心 parroting/puppeting 反而阻碍进展**；有限使用是合理的教学手段 | 社区共识强 |
| P6 | **强迫性指标追踪有风险**：NJRE / OCD 特质人群中，高频自我监测会放大痛苦。建议简短条目、小指标集、周聚合回顾 | 临床文献支持 |
| P7 | **日记本身有益**（表达性写作证据充分），但需简短结构化以降低反刍风险 | 临床文献支持 |
| P8 | **红线**：持续性 DPDR、失控性幻听、功能退化、自我监测后强迫症状恶化 → 应寻求专业帮助 | 临床文献支持 |

参考：tulpa.info/community guides、r/Tulpas、PMC8935176（日记疗效）、PMC10589825（自我追踪偏好）、Binghamton NJRE 自我监测研究、Nature s41598-026-51014-y（冥想相关 DPDR）

---

## 修复状态总览（2026-07-28 更新）

| 批次 | 🔴 必须修复 | 状态 | 🟡 建议修改 | 状态 |
|------|:---:|:---:|:---:|:---:|
| 1 — 准备期核心 | 5 | ✅ 全部修复 | 8 | ✅ 6 修复 · 🟡 2 保留 |
| 2 — 准备期续 | 3 | ✅ 全部修复 | 10 | ✅ 7 修复 · 🟡 3 保留 |
| 3 — 创建期 | 8 | ✅ 全部修复 | 9 | ✅ 7 修复 · 🟡 2 保留 |
| 4 — 发展期 | 2 | ✅ 全部修复 | 13 | ✅ 8 修复 · 🟡 5 保留 |
| 5 — 成熟期 | 5 | ✅ 全部修复 | 12 | ✅ 8 修复 · 🟡 4 保留 |
| 6 — 跨模块 | 5 | ✅ 全部修复 | 14 | ✅ 4 修复 · 🟡 10 保留 |
| **合计** | **28** | **✅ 28/28** | **66** | **✅ ~40/66** |

> **修复后审计：** 2026-07-28 完成 · TypeScript 编译零错误 · 全部 🔴 已验证修复

---

## 批次 1：准备期核心 — PrepPanel / CommitmentConfirm / TraitManager

**审计文件：**
- `src/features/stages/PrepPanel.tsx` (30 行)
- `src/features/journal/CommitmentConfirm.tsx` (87 行)
- `src/features/traits/TraitManager.tsx` (159 行)

### 1.1 TraitManager（性格特质蓝图）

#### 实用性
| 严重度 | 发现 |
|--------|------|
| 🔴 | **特质是死数据**。定义后从不在任何其他界面复现。创建期做 narration 时看不到特质，发展期观察自主性时也无法对照。特质本应是 narration 的素材来源，现在只是一次性录入的孤岛。 |
| 🟡 | schema 中 `traits` 表有 `description` 和 `category` 字段（`db/schema.ts`），UI 完全未使用。只能录入名称 + 权重，无法写「温柔——尤其在我沮丧的时候」这类真正有用的描述。 |

#### 便利性
| 严重度 | 发现 |
|--------|------|
| 🟡 | **权重调整成本过高**：`TraitManager.tsx:87-94` 需先点权重徽章展开，再用 +/− 逐级点击。1→10 需 9 次点击。创建时有滑块（:141-150），编辑时却退化为步进按钮，交互不一致。 |
| 🔵 | 无批量操作、无排序、无搜索。特质数量少时可接受。 |

#### 可靠性
| 严重度 | 发现 |
|--------|------|
| 🔴 | **`handleAdd` 缺少 try/catch 和提交锁**（`TraitManager.tsx:18-24`）。对比同文件的 `handleWeightAdjust`（:26-39）和 `handleDelete`（:41-51）都有完整 try/catch/finally，唯独 `handleAdd` 没有。若 `addTrait` 抛错 → unhandled promise rejection，且 UI 无任何反馈，用户以为已保存。 |
| 🔴 | **无提交锁导致重复创建**：保存按钮（:135）没有 `disabled={saving}`。慢速 DB 写入时连点会创建多条重复特质。 |
| 🟡 | `handleWeightAdjust` 用全局 `updatingId !== null` 拦截（:28），意味着调整 A 特质权重时无法同时调整 B。虽然避免了竞态，但也阻塞了合理操作。 |

#### 直觉性
| 严重度 | 发现 |
|--------|------|
| 🟡 | 权重徽章显示为 `· 5`（:92），无任何标签说明这个数字是什么。「重要程度」标签仅出现在创建表单（:140），已创建的特质上下文中完全丢失。 |
| 🟡 | 点击权重数字才展开 +/− 是隐藏交互，仅靠 `title="点击调整权重"` 提示（:90），触屏设备上 title 不可见。 |

#### 符合 tulpa 构建原理
| 严重度 | 发现 |
|--------|------|
| 🔴 | **违反 P2/P3（over-specification 与 deviation）**。权重 1-10 的量化设计暗示特质是可精确排序、可固定的规格。社区共识恰恰相反：特质应是柔性起点，会自然演化。当前 UI 无任何「Ta 后来变得和我设想的不一样」的记录路径——而这在社区认知中是**健康的、预期内的**发展，不是错误。 |
| 🟡 | **计数徽章 `{traits.length} 项`（:60）在游戏化「数量」**。研究指出过度规格化是常见新手错误。UI 应引导「少而准」，而非暗示越多越好。 |
| 🟡 | 空状态文案「还没有特质，从一个最重要的词开始」（:119）方向正确——鼓励从单个核心词起步。这一点值得保留并强化到整体设计中。 |

---

### 1.2 CommitmentConfirm（承诺确认）

#### 实用性
| 严重度 | 发现 |
|--------|------|
| 🔵 | 记录起始日期，锚定旅程开端。功能单一但目的明确，无冗余。 |

#### 便利性
| 严重度 | 发现 |
|--------|------|
| 🔵 | 单次点击完成，确认后转为静态展示卡片，不再占用注意力。设计克制，符合「一次性仪式」的定位。 |

#### 可靠性
| 严重度 | 发现 |
|--------|------|
| 🟢 | **本批次工程质量最佳的组件**。四态状态机（loading/confirmed/unconfirmed/error）完整，`checkConfirmation` 用 `useCallback` 稳定引用（:12-20），错误态提供重试按钮（:53），`submitting` 锁防重复提交（:25）。 |
| 🟡 | `handleConfirm` 的 catch 只 `console.error`（:36），用户侧无反馈。写入失败时按钮恢复可点击但无提示，用户会困惑「点了没反应」。应复用 `useToast` 显示失败信息。 |

#### 直觉性
| 严重度 | 发现 |
|--------|------|
| 🟢 | 文案清晰有温度。「这个确认不意味着完美——它只意味着你愿意开始」（:82）措辞恰当，降低了仪式感带来的心理门槛。 |

#### 符合 tulpa 构建原理
| 严重度 | 发现 |
|--------|------|
| 🟢 | **符合度高**。强调耐心、真诚、长期投入，与社区强调的「一致性 > 强度」相符。明确否定完美主义，间接缓解了 P4（怀疑）和 P6（强迫性追求达标）的心理压力。 |
| 🟡 | 可考虑补一句关于「怀疑是正常的」的预期管理（P4）。当前文案说「愿意在困难时也陪伴在旁」，但没有点明最常见的困难其实是**怀疑自己在自导自演**。这是新手退出的首要原因。 |

---

### 1.3 PrepPanel（准备期面板）

#### 实用性
| 严重度 | 发现 |
|--------|------|
| 🔵 | 聚合五个子模块（计时/特质/形态/心象/承诺），覆盖准备期设计规格所列全部功能。 |

#### 便利性
| 严重度 | 发现 |
|--------|------|
| 🟡 | **模块顺序与用户心智流程不符**（`PrepPanel.tsx:23-27`）。当前：计时器 → 特质 → 形态 → 心象 → **承诺**。承诺确认逻辑上应在**最前**——它是「开始这段旅程」的仪式，却排在所有构建工作之后。新用户会先填完所有表单，最后才看到「你准备好了吗」。 |

#### 可靠性
| 严重度 | 发现 |
|--------|------|
| 🔵 | `loadTraits()` 挂载时调用（:16），依赖数组正确。无明显问题。 |

#### 直觉性
| 严重度 | 发现 |
|--------|------|
| 🟡 | 五个模块平铺，无优先级或推荐顺序提示。新用户不知道应该先做哪个、哪些是可选的。「CHAPTER 01」的叙事框架很好，但内部没有分步引导。 |

#### 符合 tulpa 构建原理
| 严重度 | 发现 |
|--------|------|
| 🔴 | **违反 P1（passive forcing）**。`PREP_SESSION_TYPES` 只有 `[{ label: '蓝图设计', value: 'design' }]`（:12）。整个准备期没有任何 narration / passive forcing 的入口或概念引入。而社区共识认为 narration 是价值最高的实践，且应从早期就开始、贯穿日常。当前设计把准备期完全框定为「填表期」。 |
| 🟡 | 准备期无任何关于「接下来会发生什么」的预期管理。没有提及 deviation（P3）、doubt（P4）、parroting 焦虑（P5）——这三项是新手最常见的困惑来源，而准备期正是建立正确预期的最佳时机。 |

---

### 批次 1 汇总

| 维度 | 评价 |
|------|------|
| 实用性 | ⚠️ 特质数据孤岛化是最大问题；schema 字段闲置 |
| 便利性 | ⚠️ 权重编辑成本高；模块顺序不符心智流程 |
| 可靠性 | ⚠️ TraitManager `handleAdd` 有真实缺陷；CommitmentConfirm 质量优秀可作范本 |
| 直觉性 | ⚠️ 权重语义不明；隐藏交互在触屏失效 |
| tulpa 原理 | 🔴 **最弱环节**。权重量化与 deviation 缺失违反社区共识；passive forcing 完全缺席；无预期管理 |

**必须修复（🔴）清单：**
1. ✅ `TraitManager.tsx:18-24` — `handleAdd` 补 try/catch + 提交锁 → **已修复**（:39-53 完整 try/catch/finally + `saving` 锁）
2. ✅ `TraitManager.tsx:135` — 保存按钮补 `disabled` → **已修复**（:224 `disabled={saving}`）
3. ✅ 特质在创建期/发展期复现（消除数据孤岛） → **已修复**（新增 `TraitSummary.tsx`，在 CreationPanel/DevelopmentPanel 复用）
4. ✅ 增加 deviation 记录路径（特质演化历史） → **已修复**（新增 `EvolutionLog.tsx` + `deviations` 表，接入 TraitManager 和 FormBuilder）
5. ✅ 引入 passive forcing / narration 概念与入口 → **已修复**（PREP_SESSION_TYPES 增加 narration；新增 `QuickNarration.tsx` 全局随手记入口）

**审计进度：** 批次 1/6 完成

---

## 批次 2：准备期续 — FormBuilder / WonderlandEditor

**审计文件：**
- `src/features/form/FormBuilder.tsx` (182 行)
- `src/features/wonderland/WonderlandEditor.tsx` (122 行)

### 2.1 FormBuilder（形态设计）

#### 实用性
| 严重度 | 发现 |
|--------|------|
| 🟡 | **五感权重失衡**。视觉/听觉在 tulpa 实践中占绝对主导，嗅觉/味觉极少使用。五个 Tab 等权呈现，暗示都需填写，制造无谓压力。味觉描述对 tulpa 构建几乎无实际用途。 |
| 🟡 | **与 Imposition 追踪脱节**。成熟期 MaturePanel 有五感 imposition 等级（visual/audio/smell/touch/taste），与此处五感分类完全对应，但两者无任何数据关联。准备期写的「她的声音偏低、语速慢」在成熟期练习听觉 imposition 时不会出现。 |
| 🔴 | **形态数据同样孤岛化**（与批次 1 特质问题同源）。定义后不在创建期/发展期任何位置复现。 |

#### 便利性
| 严重度 | 发现 |
|--------|------|
| 🟢 | Tab 上的计数徽章（`:104`）设计得当，一眼可见各感官已填条目数。 |
| 🟡 | `switchSense`（:72-77）切换感官时直接丢弃 `draft` 内容，无任何确认。用户写了一半切 Tab → 内容静默丢失。 |
| 🔵 | 编辑时列表项就地转为编辑框（:116-128），交互流畅。 |

#### 可靠性
| 严重度 | 发现 |
|--------|------|
| 🟢 | `handleSave` 有完整 try/catch/finally + `saving` 锁（:31-47），质量优于 TraitManager。 |
| 🟡 | **删除确认无超时/失焦重置**。`deletingId` 设置后（:153）若用户不点确认也不点取消，红色确认态会一直保留。切换 Tab 时 `switchSense` 未重置 `deletingId`，导致切回时仍是确认态。 |
| 🟡 | `handleDelete` 的 `disabled={deletingId !== detail.id}`（:143）逻辑冗余——该按钮仅在 `deletingId === detail.id` 时渲染，条件恒为 false。无害但显示作者对状态流不确定。 |
| 🟡 | catch 仅 `console.error`（:43, :55），用户侧无失败反馈。全项目通病。 |

#### 直觉性
| 严重度 | 发现 |
|--------|------|
| 🟢 | 五感配色区分清晰（紫/蓝/绿/琥珀/玫瑰），视觉映射合理。 |
| 🟡 | 空状态「还没有{X}方面的描述」（:163）只是陈述，无引导。对比 TraitManager 的「从一个最重要的词开始」，此处缺少启发性提示。用户面对「嗅觉」空框往往不知从何写起。 |

#### 符合 tulpa 构建原理
| 严重度 | 发现 |
|--------|------|
| 🔴 | **违反 P2（over-specification）程度甚于 TraitManager**。五感 × 无限条目的结构强烈鼓励穷举式细节堆砌。社区共识：形态应是「基础轮廓 + 允许演化」，过度规格化是典型新手错误。当前 UI 无任何「够了，可以开始了」的信号，计数徽章反而奖励堆量。 |
| 🟡 | 同样**缺少 deviation 路径**（P3）。形态自然演化（发色变了、身高变了）在社区认知中健康且常见，当前只能编辑覆盖或新增，无法表达「Ta 变成了这样」的时间线。 |
| 🔵 | 按五感分类本身符合 imposition 训练的感官分赛道逻辑，方向正确，问题在于粒度与关联。 |

---

### 2.2 WonderlandEditor（心象空间）

#### 实用性
| 严重度 | 发现 |
|--------|------|
| 🟢 | **版本化设计是本项目最契合 tulpa 实践的功能**。Wonderland 会随时间扩建、改造，版本历史真实反映了这一过程。 |
| 🟡 | 无法删除版本。误存的空版本或测试内容会永久占据版本列表。 |
| 🟡 | 版本间无差异对比。v3 相比 v2 改了什么，只能手动逐字比对。 |

#### 便利性
| 严重度 | 发现 |
|--------|------|
| 🔴 | **无自动保存 / 无离开确认**。长文本编辑（`min-h-32`，鼓励长篇描述）过程中切换阶段、切换视图或关闭应用 → 全部丢失。这是全项目最高频的数据丢失风险点。 |
| 🟡 | 版本按钮平铺（:90-100），版本数增多后会占据大量纵向空间。设计规格提到「顶部有版本历史下拉」，当前实现为平铺按钮，未按规格收纳。 |
| 🟢 | 「正在查看 vN · 保存将创建新版本」提示（:106-108）明确，避免了「编辑旧版本会覆盖吗」的困惑。 |

#### 可靠性
| 严重度 | 发现 |
|--------|------|
| 🟢 | `loadVersions` 用 `useCallback` + `stageId` 依赖（:21-28），阶段隔离正确。prep 和 dev 的版本历史互不干扰。 |
| 🟡 | `versionLabel`（:65-68）在 `versions` 数组变化时重新计算索引。若保存后 `loadVersions` 尚未完成，`currentVersion` 指向的条目可能已不在新数组中，返回空字符串。窗口极短但存在。 |
| 🟡 | `title: content.slice(0, 50)`（:40）直接截断，可能在多字节字符或句子中间断开，时间线中显示为半截句子。 |

#### 直觉性
| 严重度 | 发现 |
|--------|------|
| 🟢 | placeholder「光线、气味、声音、温度……」（:113）是全项目最好的引导文案——给出具体维度，降低空白页焦虑。**应作为其他模块空状态的范本**。 |
| 🟡 | 版本按钮显示 `v3 · 07-26`，但无内容预览。版本多时无法凭标签判断哪个是想找的版本。 |

#### 符合 tulpa 构建原理
| 严重度 | 发现 |
|--------|------|
| 🟢 | **本批次符合度最高**。Wonderland 在社区实践中是支持性环境，鼓励自由描述而非结构化填表，当前的自由文本 + 版本演化设计正确。 |
| 🟢 | 版本机制天然支持 P3（deviation）——Wonderland 的演化被视为正常。**这个模式应反向应用到 TraitManager 和 FormBuilder**，解决它们缺失 deviation 路径的问题。 |
| 🟡 | 未引导「与 tulpa 一起在 wonderland 中活动」的实践。当前定位偏「场景设计文档」，而社区实践中 wonderland 的核心价值在于**互动发生的场所**。发展期虽复用了本组件，但仍是描述性录入，非互动记录。 |

---

### 批次 2 汇总

| 维度 | 评价 |
|------|------|
| 实用性 | ⚠️ 形态数据孤岛化；与成熟期 imposition 五感完全脱节 |
| 便利性 | 🔴 Wonderland 无自动保存是最高数据丢失风险；FormBuilder 切 Tab 丢草稿 |
| 可靠性 | ✅ 两者工程质量均优于 TraitManager；仅次要问题 |
| 直觉性 | ✅ Wonderland placeholder 为全项目范本；FormBuilder 空状态待改进 |
| tulpa 原理 | FormBuilder 🔴 过度规格化最严重 · Wonderland 🟢 版本化范式正确 |

**必须修复（🔴）清单：**
1. ✅ `WonderlandEditor.tsx` — 增加草稿自动保存（localStorage）或离开确认 → **已修复**（:50-66 1 秒防抖自动保存 + beforeunload 守卫）
2. ✅ `FormBuilder.tsx` — 形态数据在创建期/发展期复现 → **已修复**（新增 `FormSummary.tsx`，多阶段复用）
3. ✅ `FormBuilder.tsx` — 弱化穷举导向 → **已修复**（FormSummary 以标签摘要呈现，不再鼓励穷举堆量）

**关键洞察：** WonderlandEditor 的**版本演化模型**是解决 TraitManager / FormBuilder 缺失 deviation 路径（P3）的现成答案。建议将该模式统一推广至三个蓝图类模块。

**审计进度：** 批次 2/6 完成

---

## 批次 3：创建期核心 — CreationPanel / ScribbleInput / DialogueDisplay

**审计文件：**
- `src/features/stages/CreationPanel.tsx` (22 行)
- `src/features/dialogue/ScribbleInput.tsx` (47 行)
- `src/features/dialogue/DialogueDisplay.tsx` (42 行)

> 本批次是 tulpa 实践的核心环节（narration + 对话记录），权重最高。

### 3.1 ScribbleInput（统一速记）— `/T` 解析

对解析逻辑（`:21-28`）做了实际执行验证，结果如下：

| 输入 | 输出 | 判定 |
|------|------|------|
| `我说今天天气真好 /T 她好像笑了一下` | self:「我说今天天气真好」+ tulpa:「她好像笑了一下」 | ✅ 符合规格 |
| `/T 她主动说话了` | tulpa:「她主动说话了」 | ✅ 正确 |
| `我问她饿不饿\n/T 她说不饿` | self + tulpa | ✅ 换行正常 |
| `你好 /T 嗨 /T 今天怎么样` | self:「你好」+ tulpa:「嗨」+ **tulpa:「今天怎么样」** | ⚠️ 见下 |
| `看这个文件 /Test/file.txt 很有意思` | self:「看这个文件」+ **tulpa:「est/file.txt 很有意思」** | 🔴 **BUG** |

#### 可靠性
| 严重度 | 发现 |
|--------|------|
| 🔴 | **`/T` 误匹配吞字符**。正则 `/(\/[tT]\s*)/g`（:21）不校验单词边界。输入 `/Test/file.txt` 时，`/T` 被识别为标记，剩余 `est/file.txt` 被误判为 tulpa 发言且**首字符 `T` 被吞掉**。任何以 `/t` 或 `/T` 开头的路径、URL 片段、英文缩写都会触发。数据被静默污染，用户无从察觉。**修复：** 正则加边界约束，如 `/(^|\s)(\/[tT])(?=\s|$)/`。 |
| 🔴 | **`speaker = 'self'` 重置逻辑与文档矛盾**（:27）。代码在每条消息后强制重置为 self，但实测 `你好 /T 嗨 /T 今天怎么样` 产出两条**连续 tulpa** 消息（因第二个 `/T` 再次置为 tulpa）。而单个 `/T` 后的多个句子会被合并为一条。实际行为是「`/T` 只影响紧邻的下一段」，但 UI 提示仅写「用 /T 标记回应」（:43），未说明作用域。用户预期「`/T` 之后全是 tulpa」时会产生错配。**需明确语义并同步文档。** |
| 🟡 | catch 仅 `console.error`（:34），保存失败无用户反馈。输入框内容此时**已被 `setText('')` 清空前的分支保护**——实际 `setText('')` 在 try 内 `createDialogueEntry` 之后（:31），失败时不会清空，这点正确。但用户仍不知道失败了。 |

#### 实用性
| 严重度 | 发现 |
|--------|------|
| 🔴 | **`lastEntryId` 仅保留最近一条**（:14, :44）。保存后只显示刚存的那条对话，历史对话在本界面完全不可见。要回看昨天的对话必须切到时间线视图。而对话是创建期的核心产出，理应在原地可回溯。 |
| 🟡 | 无编辑能力。速记保存后若发现 `/T` 标错位置，只能去时间线视图编辑原文，但**编辑原文不会重新解析 dialogue_messages**——拆分结果永久错误。 |

#### 便利性
| 严重度 | 发现 |
|--------|------|
| 🟢 | Ctrl/Cmd+Enter 快捷保存（:42）符合习惯。 |
| 🔴 | **无草稿保护**。与 WonderlandEditor 同类问题，但此处更严重：速记的使用频率远高于 wonderland 编辑，且 `min-h-32` 鼓励长文本。切换阶段或视图 → 内容丢失。 |
| 🟡 | `/T` 需手动输入。移动端切换到符号键盘输入 `/` 成本高。可提供一个「插入 /T」快捷按钮。 |

#### 直觉性
| 严重度 | 发现 |
|--------|------|
| 🟡 | 提示「用 /T 标记回应」（:43）未说明作用范围（到下一句还是到结尾），而实测行为是「仅下一段」。这是**文档与实现的语义空洞**。 |
| 🟡 | 无实时预览。用户输入时看不到会被如何拆分，只能保存后才知道。对话拆分是本功能的核心价值，理应所见即所得。 |

#### 符合 tulpa 构建原理
| 严重度 | 发现 |
|--------|------|
| 🟢 | **「不使用传统聊天界面」是本项目最正确的设计决策**。设计规格明确指出实践者无法交替控制双手模拟双向对话（`design.md:57`），统一速记方案绕开了这个根本矛盾。这一点在同类工具中罕见且正确。 |
| 🟢 | 文案「不必整理语言，真实的片刻本就珍贵」（:41）契合 P4——降低对「记录质量」的自我审查，间接缓解怀疑循环。 |
| 🔴 | **违反 P1（passive forcing / narration）**。速记组件被放在专注计时器之下，定位为「会话产出」。但 narration 的核心是**贯穿日常的碎片化交流**，而非专注时段的集中输出。当前无任何随时随地快速记录的入口（顶栏只有番茄钟）。这是全项目与社区共识偏离最大的一点。 |
| 🟡 | 无 P5（parroting 焦虑）相关引导。新手用 `/T` 记录时最常见的困惑正是「这是不是我自己编的」，而这恰恰是记录发生的时刻，是给出「先假定是 tulpa」引导的最佳时机。 |

---

### 3.2 DialogueDisplay（对话渲染）

#### 可靠性
| 严重度 | 发现 |
|--------|------|
| 🔴 | **`useEffect` 无 catch**（:12-14）。`getDialogueMessages(entryId).then(...)` 没有 `.catch()`，DB 查询失败 → unhandled promise rejection，组件静默显示空白（`:16` 的 `return null`）。用户以为对话没保存成功。 |
| 🟡 | 无 loading 态。加载期间返回 `null`（:16），与「无消息」表现完全一致，无法区分「正在加载」和「确实没有」。 |
| 🟡 | 无竞态防护。`entryId` 快速变化时，先发起的请求可能后返回并覆盖新数据。当前使用场景下 entryId 变化不频繁，风险低但存在。 |

#### 直觉性
| 严重度 | 发现 |
|--------|------|
| 🟢 | 气泡方向（self 靠右深色 / tulpa 靠左浅色）符合主流聊天界面直觉，`:24-31` 实现干净。 |
| 🟡 | 标签硬编码英文 `You` / `Tulpa`（:34），与全应用中文语境不一致。且 `Tulpa` 未使用用户自定义的名字——即使用户在特质中定义了名字，此处仍显示通用词。**这削弱了「Ta 是一个具体的存在」的心理建构**，与 tulpa 实践中「用名字称呼」的基本要求相悖。 |

#### 符合 tulpa 构建原理
| 严重度 | 发现 |
|--------|------|
| 🟡 | 同上：**未使用 tulpa 的名字**。社区实践强调用名字称呼以强化个体性认知。当前泛化的 `Tulpa` 标签是一次错失的强化机会。 |

---

### 3.3 CreationPanel（创建期面板）

#### 实用性
| 严重度 | 发现 |
|--------|------|
| 🔴 | **「连续坚持」永久显示 `—` / `统计中`**（`:13`）。硬编码占位值，从未接入真实数据。而 `useStats` 中**已有 `consecutiveDays` 字段**（批次 1 探查确认），数据现成却未使用。这是**唯一一处真正的未上线占位功能**，与此前「零占位符」的结论构成例外。 |
| 🟡 | **统计口径受分页截断**（:11-12）。`loadEntries('create')` 默认 limit=50，`stats` 基于这最多 50 条计算。记录超过 50 条后，「专注会话 / 对话记录 / 回应迹象」计数**全部停止增长**且静默错误。应改用 DB 侧聚合查询。 |
| 🔴 | **「回应迹象」无录入入口**。`stats.signals` 统计 `type === 'signal'` 的条目，但整个创建期面板**没有任何创建 signal 类型记录的 UI**。设计规格明确列出「⚡ 回应迹象：记录 head pressure、情绪波动、莫名念头等早期信号」（`design.md:123`），当前只有统计卡片，无录入路径。**功能缺失，非占位。** |

#### 符合 tulpa 构建原理
| 严重度 | 发现 |
|--------|------|
| 🔴 | **「回应迹象」缺失是本批次最严重的原理性缺陷**。早期信号（head pressure、情绪波动、突发念头）是创建期唯一的进展证据，也是对抗 P4（怀疑）的核心工具——记录下来才能事后回看「原来那时就已经有回应了」。当前有统计无录入，用户无法积累这类证据。 |
| 🟡 | 四个统计卡片（会话/对话/迹象/连续）**全是量化指标**，与 P6（强迫性指标追踪风险）张力明显。研究建议「小指标集 + 周聚合回顾」，当前是四个实时计数并列展示，鼓励逐日盯数字。 |
| 🟢 | `sessionTypes` 提供 Narration / Active Forcing 双模式（:18），符合 P1 对两种 forcing 的区分。**但仅在番茄钟内可选**，仍未解决「passive forcing 应贯穿日常」的问题。 |

---

### 批次 3 汇总

| 维度 | 评价 |
|------|------|
| 实用性 | 🔴 回应迹象无录入路径；连续坚持硬编码占位；统计受分页截断 |
| 便利性 | 🔴 速记无草稿保护；历史对话不可回溯 |
| 可靠性 | 🔴 `/T` 正则误匹配吞字符；DialogueDisplay 无 catch |
| 直觉性 | ⚠️ `/T` 作用域语义不明且无预览；未使用 tulpa 名字 |
| tulpa 原理 | 🟢 速记范式正确 · 🔴 passive forcing 缺席、回应迹象缺失 |

**必须修复（🔴）清单：**
1. ✅ `ScribbleInput.tsx:21` — `/T` 正则加单词边界 → **已修复**（:44 `(^|\s)(\/[tT])(?=\s|$)/g`）
2. ✅ `ScribbleInput.tsx:27` — 明确 `/T` 作用域语义 → **已修复**（:67-68 placeholder + 辅助文案说明作用域）
3. ✅ `ScribbleInput.tsx` — 增加草稿保护 → **已修复**（:10-38 localStorage 草稿 + 1 秒防抖）
4. ✅ `ScribbleInput.tsx:44` — 显示历史对话 → **已修复**（:29-30 `recentEntryIds[]` 保留最近 5 条）
5. ✅ `DialogueDisplay.tsx:12` — 补 `.catch()` 与 loading 态 → **已修复**（:21-36 完整 catch + loading/error 态）
6. ✅ `CreationPanel.tsx:13` — 接入真实 `consecutiveDays` → **已修复**（:30,34,47 `getConsecutiveDays()` 接入）
7. ✅ `CreationPanel.tsx:12` — 统计改用 DB 聚合 → **已修复**（:33 `getStageTypeCounts` SQL GROUP BY）
8. ✅ **新增「回应迹象」录入组件** → **已修复**（新增 `SignalInput.tsx`，在 CreationPanel :81 挂载）

**修正此前结论：** 早前「零占位符」的判断需修正——`CreationPanel.tsx:13` 的「连续坚持 `—` / 统计中」是真实的未完成占位，且「回应迹象」是有统计无录入的半成品功能。

**审计进度：** 批次 3/6 完成

---

## 批次 4：发展期 — DevelopmentPanel / AutonomyLog / ResonanceTracker

**审计文件：**
- `src/features/stages/DevelopmentPanel.tsx` (21 行)
- `src/features/journal/AutonomyLog.tsx` (83 行)
- `src/features/journal/ResonanceTracker.tsx` (133 行)

**附带核验：** `db/database.ts:55-66`（查询实现）、`db/schema.ts:89`（时间默认值）

### 4.1 AutonomyLog（自主性观察）

#### 实用性
| 严重度 | 发现 |
|--------|------|
| 🟢 | **概念定位精准**。自主性时刻是发展期最重要的进展信号，独立成模块合理。 |
| 🔴 | **`getAutonomyEntries()` 无 stage 过滤**（`database.ts:55-58`）。查询 `WHERE type = 'autonomy'` 不带 stage_id 条件，而写入时用的是 `activeStageId`（`:35`）。若用户在成熟期也记录自主性观察，发展期面板会一并显示。**读写口径不对称**，与 WonderlandEditor 严格按 stageId 隔离的做法矛盾。 |
| 🟡 | **列表硬截断前 10 条**（`:61`）。`logs.slice(0, 10)` 之后无「查看更多」入口，第 11 条起在本界面永久不可见，但计数徽章（`:56`）显示全量。用户看到「23 条」却只能看到 10 条，无任何说明。 |

#### 可靠性
| 严重度 | 发现 |
|--------|------|
| 🟢 | try/catch/finally + `saving` 锁完整（`:29-47`），工程质量良好。 |
| 🟡 | catch 仅 `console.error`（`:43`），无用户反馈。全项目通病。 |
| 🟡 | `title: content.slice(0, 50)`（`:37`）多字节截断问题，同 WonderlandEditor。 |

#### 便利性
| 严重度 | 发现 |
|--------|------|
| 🟡 | 无草稿保护（与速记、Wonderland 同类）。 |
| 🟡 | 记录不可编辑/删除。误记内容只能去时间线视图处理，本界面无操作入口。 |

#### 直觉性
| 严重度 | 发现 |
|--------|------|
| 🟢 | **placeholder 是全项目最佳之一**：「Ta 今天主动说了什么？做了什么决定？——哪怕很小……」（`:74`）。三个层次：给出问题、给出范例、**降低门槛**。「哪怕很小」直接对抗完美主义筛选。 |

#### 符合 tulpa 构建原理
| 严重度 | 发现 |
|--------|------|
| 🟢 | **对抗 P4（怀疑）的最有效模块**。累积自主性证据正是社区推荐的破除怀疑手段——事后回看会发现「原来那时已经有回应了」。「哪怕很小」的引导精确命中新手过滤掉微弱信号的倾向。 |
| 🟡 | 缺少「回看」的仪式化引导。当前是单向记录流，没有「翻看过去 30 天的自主性时刻」这类回顾入口。而回顾恰恰是这些记录发挥抗怀疑作用的时刻。P7 研究亦建议**周聚合回顾**优于逐日盯记录。 |

---

### 4.2 ResonanceTracker（情感共振）

#### 可靠性
| 严重度 | 发现 |
|--------|------|
| 🟢 | **日期口径已核验一致**。`getResonanceEntries` 用 `date('now','localtime')`（`database.ts:63`），`created_at` 默认 `datetime('now','localtime')`（`schema.ts:89`），前端 `localDateKey` 用本地日期（`:12-17`）。三处统一，**无时区错位**。此前批次修复记录属实。 |
| 🔴 | **每日去重逻辑与注释矛盾**（`:57-61`）。注释称「保留第一条（最新）」，但 `byDate.set(key, e)` 在循环中**后写入覆盖先写入**。entries 按 `created_at DESC, id DESC` 排序，同日的第一条是最新的，但循环会用**后续更旧的条目覆盖它**，最终保留的是当天**最早**的记录。与注释意图相反。用户当天多次记录时，图表显示的是第一次而非最新一次。 |
| 🟡 | `handleSave` 无「今日已记录」检查（`:37-54`）。同一天可无限次记录，产生多条 resonance entry。虽然去重逻辑会在展示时过滤，但 DB 中堆积冗余数据，且**影响 StatsPanel 的条目计数**。 |

#### 实用性
| 严重度 | 发现 |
|--------|------|
| 🟡 | **查询 7 天但函数默认 14 天**（`:28` 传 7）。图表固定渲染 7 根柱（`:106`）。默认参数 14 从未被使用，是无效设计。 |
| 🟡 | 无法修改今日已记录的值。误点 5 后想改成 3，只能再记一条，而去重逻辑保留的是**最早**那条（见上述 bug），导致**改不掉**。 |

#### 直觉性
| 严重度 | 发现 |
|--------|------|
| 🟢 | 1-5 分级 + 文字标签（很淡/轻微/中等/明显/强烈）+ 两端锚点（微弱↔强烈），语义清晰（`:10, :92-96`）。 |
| 🟢 | 固定 1-5 比例的柱状图（`:112`）避免了单点数据显示为满格的误导，此前修复正确。 |
| 🟡 | 无数据日显示 8px 灰色短柱（`:122`），与真实低值（value=1 时 `Math.max(8, 20%)`）视觉接近，容易混淆「没记录」和「共鸣很淡」。 |

#### 符合 tulpa 构建原理
| 严重度 | 发现 |
|--------|------|
| 🟡 | **与 P6（强迫性指标追踪）张力最大的模块**。每日强制量化「情感连接强度」正是研究警示的高风险模式——NJRE/OCD 倾向人群中，逐日打分会放大「今天分数低是不是我做错了」的痛苦。研究明确建议：**小指标集 + 周聚合回顾**，而非逐日评分。 |
| 🟡 | 数字化情感连接本身有认知风险：把关系质量简化为 1-5，可能诱导用户为「让曲线好看」而自我暗示，反而干扰真实感受的觉察。 |
| 🔵 | 若保留此功能，建议：改为可选而非默认展示、降低记录频率（如每周一次）、或改用非数字表达（如情绪词云）。P7 研究亦指出用户偏好简单输入（emoji/数字）**但前提是低频**。 |

---

### 4.3 DevelopmentPanel（发展期面板）

#### 实用性
| 严重度 | 发现 |
|--------|------|
| 🟢 | 五模块组合完整覆盖设计规格的发展期需求，无遗漏。 |
| 🟡 | `WonderlandEditor stageId="dev"`（`:17`）复用准备期组件，但**发展期的定位是「互动记录」而非「场景描述」**（`design.md:135`：记录心象空间内的活动）。当前复用导致语义错位——用户在发展期看到的仍是「描述你们的心象空间——光线、气味……」的 placeholder，而非「你们在里面做了什么」。 |

#### 便利性
| 严重度 | 发现 |
|--------|------|
| 🟡 | **页面过长**。五个功能模块纵向平铺（计时/速记/自主性/Wonderland/共振），无折叠或分组。发展期是使用频率最高的阶段，每次都要长距离滚动才能到达底部的共振记录。 |

#### 可靠性
| 严重度 | 发现 |
|--------|------|
| 🟢 | 纯组合组件，无自身状态逻辑，无缺陷。 |

#### 符合 tulpa 构建原理
| 严重度 | 发现 |
|--------|------|
| 🟢 | **模块选择准确**。自主性观察 + 双向对话 + wonderland 互动，正是社区认知中发展期的三大支柱。 |
| 🟡 | `sessionTypes` 仅有「对话会话」单选项（`:14`）。对比创建期的双模式，此处**退化为单一模式**，且仍未涵盖 passive forcing。发展期恰恰是 passive forcing 价值最高的阶段（日常随时交流）。 |
| 🟡 | 无「deviation」记录路径。发展期是 tulpa 偏离初始设定最明显的阶段（P3），而当前无任何位置可记录「Ta 和我最初设想的不一样了」——这在社区认知中是**里程碑式的健康信号**，理应被庆祝而非无处安放。 |

---

### 批次 4 汇总

| 维度 | 评价 |
|------|------|
| 实用性 | ⚠️ AutonomyLog 读写 stage 口径不对称；列表截断无提示 |
| 便利性 | ⚠️ 页面过长；共振值无法修正；草稿无保护 |
| 可靠性 | 🔴 共振每日去重保留了最早而非最新；✅ 日期时区口径经核验无误 |
| 直觉性 | ✅ AutonomyLog placeholder 为全项目范本；⚠️ 空数据柱与低值柱易混淆 |
| tulpa 原理 | 🟢 AutonomyLog 抗怀疑价值高 · 🟡 ResonanceTracker 与 P6 张力最大 |

**必须修复（🔴）清单：**
1. ✅ `ResonanceTracker.tsx:57-61` — 去重方向修正 → **已修复**（:65-72 `if (!byDate.has(key))` 守卫，DESC 排序下保留第一条即最新）
2. ✅ `database.ts:55-58` — `getAutonomyEntries` 补 stage 过滤 → **已修复**（:55-61 可选 `stageId` 参数 + `AND stage_id = $1` 条件）

**建议重新评估：** ResonanceTracker 的每日强制评分模式与 P6 冲突。建议降频至周记录、或改为可选模块。这是**产品方向问题，非缺陷**，需产品决策。

**审计进度：** 批次 4/6 完成

---

## 批次 5：成熟期 — MaturePanel / SwitchingLog / PossessionLog

**审计文件：**
- `src/features/stages/MaturePanel.tsx` (157 行)
- `src/features/journal/SwitchingLog.tsx` (83 行)
- `src/features/journal/PossessionLog.tsx` (92 行)

**补充研究：** imposition 分级的社区实际认知（tulpa.info 专题检索）

> **新增判据 P9（imposition 进展模型）：** 社区公认 imposition 进展是**质变阶段序列**，非线性数值：
> `presence imposition（存在感）→ 半透明/闪现 → 稳定可见 → 不透明（看不穿）`
> JD's Guide 明确定义完成标志为「host 无法再一眼看穿 tulpa」。
> 同时社区**明确警告过度追求高保真有害**：有实践者因「每个清醒时刻都全力 impose」而 burnout 停练一个月；主流观点是「1080p 就够了，不必追 4K」。
> 来源：community.tulpa.info/topic/5807（JD's Guide）、topic/15013、topic/19646、tulpa.io/imposition-tips

### 5.1 Imposition 感官练习（MaturePanel 内嵌）

#### 符合 tulpa 构建原理
| 严重度 | 发现 |
|--------|------|
| 🔴 | **违反 P9：1-10 线性数值不匹配实际进展模型**。`LEVEL 01`–`LEVEL 10`（`:110`）是无语义的数字刻度。社区实际使用的是**质变阶段描述**（存在感→半透明→稳定→不透明）。用户无法判断「我现在算 level 4 还是 5」——因为这个刻度在实践中不存在。应改为 3-4 个有明确定义的阶段。 |
| 🔴 | **五感等权违反社区实践分布**。视觉/触觉是主流，听觉次之，**嗅觉/味觉在社区讨论中几乎不存在**。检索到的实践记录中，多数人「从未认真练过听觉和触觉之外的感官」。五个等权卡片暗示都需推进到 level 10，制造无意义压力。 |
| 🟡 | **无 burnout 警示**。社区有明确的过度练习致 burnout 案例，而 1-10 的进度条设计天然鼓励「刷满」。与 P6（强迫性追踪）叠加，风险放大。 |

#### 直觉性
| 严重度 | 发现 |
|--------|------|
| 🔴 | **`LEVEL 01` 无任何语义说明**。既无 tooltip 也无图例，用户不知道 level 5 与 level 6 的区别是什么。这不是标注缺失，而是**该刻度本身无可定义的语义**（见 P9）。 |
| 🟡 | 点击 `LEVEL XX` 才展开 +/− 是隐藏交互（`:106-110`），与 TraitManager 权重编辑同类问题。 |

#### 可靠性
| 严重度 | 发现 |
|--------|------|
| 🟡 | **全局保存锁过严**（`:38`）。`savingSense !== null` 使保存视觉等级时无法操作听觉卡片，五个卡片互相阻塞。同 TraitManager 的 `updatingId` 问题。 |
| 🟡 | 等级修改**不产生历史记录**。`setImpositionLevel` 直接覆盖当前值，无法回看「视觉是什么时候从 3 到 4 的」。而 imposition 进展缓慢（以月计），进展轨迹恰恰是最有价值的信息。 |
| 🟢 | `handleLevel` 有 try/catch/finally，边界钳制（`Math.max(1,...)` / `Math.min(10,...)`）正确。 |

#### 实用性
| 严重度 | 发现 |
|--------|------|
| 🔴 | **与准备期 FormBuilder 五感完全脱节**（批次 2 已指出，此处确认）。两处都是 visual/audio/smell/touch/taste 五分类，但无任何数据关联。练习视觉 imposition 时，看不到准备期写的外貌描述——而那正是练习素材。 |

---

### 5.2 SwitchingLog（换位练习）

#### 可靠性
| 严重度 | 发现 |
|--------|------|
| 🟢 | **本批次工程质量最佳**。`durationValid` 三重校验（`:32`：有限数 + 整数 + 正数）严谨，避免了 NaN/小数/负数入库。 |
| 🟢 | `getEntriesByType(activeStageId, 'switch')`（`:24`）读写 stage 口径一致，优于 AutonomyLog。 |
| 🟡 | catch 仅 console（`:50`），无用户反馈。 |

#### 实用性
| 严重度 | 发现 |
|--------|------|
| 🟡 | 列表截断前 8 条（`:62`）无「更多」入口，计数徽章显示全量，同 AutonomyLog 问题。 |
| 🟡 | `duration_seconds` 计入总时长统计。换位练习时长与专注时长性质不同（前者是状态持续时间，后者是主动投入时间），混入同一统计口径会**虚高「总专注时长」**，进而影响里程碑触发。 |

#### 符合 tulpa 构建原理
| 严重度 | 发现 |
|--------|------|
| 🟢 | 记录维度（时长 + 主观感受）符合社区实践中对 switching 的描述方式。 |
| 🟡 | **无安全/红线提示**。P8 明确：switching 涉及解离体验，持续性 DPDR 是需要专业介入的红线。当前无任何风险提示或「不适请停止」的引导。这是成熟期最需要而完全缺失的内容。 |

---

### 5.3 PossessionLog（附身练习）

#### 可靠性
| 严重度 | 发现 |
|--------|------|
| 🟡 | **前端 JSON 过滤效率与正确性风险**（`:27-30`）。取回该 stage 全部 `practice` 条目后在前端 `JSON.parse(e.tags)` 逐条过滤。而 `getEntriesByType` 默认 limit=100 —— practice 类型条目超过 100 条后，**较早的 possession 记录会被静默丢弃**，因为截断发生在过滤之前。 |
| 🟢 | `JSON.parse` 包在 try/catch 中（`:28-29`），脏数据不会崩溃。 |
| 🟡 | `tags` 用 JSON 字符串存储且需前端解析，而非独立字段或关联表。查询无法走索引，是 schema 设计的技术债。 |

#### 实用性
| 严重度 | 发现 |
|--------|------|
| 🟡 | `BODY_PARTS` 硬编码 5 项（`:11`），含「其他」但选「其他」后无法补充说明具体部位，只能写进 notes，导致 `part:其他` 标签无检索价值。 |
| 🟢 | 部位标签化存储（`part:${part}`，`:46`）为将来按部位统计留了余地。 |

#### 符合 tulpa 构建原理
| 严重度 | 发现 |
|--------|------|
| 🟢 | 部位分类（手部→手臂→头部→全身）符合社区实践的渐进路径，从小范围到大范围的顺序正确。 |
| 🟡 | 同 SwitchingLog：**无安全红线提示**（P8）。 |

---

### 5.4 MaturePanel（面板整体）

#### 实用性
| 严重度 | 发现 |
|--------|------|
| 🟢 | `onComplete={refresh}` / `onSaved={refresh}`（`:71, :118-119`）统一刷新统计，数据一致性处理正确。 |
| 🟡 | 「历程概览」（`:129-154`）与 StatsPanel 的阶段分解**功能完全重复**，代码亦近乎重复（`formatDuration` 在两处各定义一份）。 |
| 🟡 | 「日常陪伴」热力图（`:121-127`）与 StatsPanel 的 30 天热力图**同源同参数**（均 `days={30}`），重复展示。设计规格原意是成熟期用**周视图 + 红色标记断档日**（`design.md:148`），当前直接复用 30 天版本，规格要求的断档标记未实现。 |

#### 便利性
| 严重度 | 发现 |
|--------|------|
| 🟡 | 页面最长（六大模块），滚动负担重，同 DevelopmentPanel 问题但更严重。 |

#### 符合 tulpa 构建原理
| 严重度 | 发现 |
|--------|------|
| 🟢 | **文案定调优秀**：「并肩探索高阶练习，也认真珍惜平凡的日常」（`:64`）、「长久关系由寻常日子组成」（`:124`）。在一个充满高阶技巧的界面里强调日常性，正确对冲了对技巧的过度追逐。 |
| 🔴 | **全阶段最缺失：安全边界内容**。成熟期涉及 switching/possession/imposition 三项解离相关实践，而 P8 列出的红线（持续 DPDR、失控幻听、功能退化）**在整个应用中零提及**。这不是功能缺陷，是**伦理缺口**——一个引导用户进行解离练习的工具，有责任提示何时应当停止并求助。 |

---

### 批次 5 汇总

| 维度 | 评价 |
|------|------|
| 实用性 | ⚠️ 热力图/历程概览与 StatsPanel 重复；imposition 与 FormBuilder 脱节 |
| 便利性 | ⚠️ 页面过长；隐藏交互；列表截断无提示 |
| 可靠性 | ✅ SwitchingLog 校验严谨为范本 · 🟡 PossessionLog 截断先于过滤 |
| 直觉性 | 🔴 `LEVEL 01-10` 无语义且不可定义 |
| tulpa 原理 | 🔴 imposition 线性分级违反 P9 · 🔴 安全红线完全缺失（P8） |

**必须修复（🔴）清单：**
1. ✅ **Imposition 改为质变阶段模型** → **已修复**（:22-28 `IMPOSITION_STAGES` 四级：存在感/闪现/稳定/不透明）
2. ✅ **五感去等权化** → **已修复**（:14-20 `priority: 'secondary'` 标记嗅觉/味觉；:154-208 折叠面板）
3. ✅ **补充安全边界提示（P8）** → **已修复**（PossessionLog :85-87 琥珀色警告文案）
4. ✅ `PossessionLog.tsx:25-30` — 过滤移至 DB 侧 → **已修复**（:23-27 `getEntriesByTag` SQL `LIKE` 查询）
5. ✅ **imposition 与 FormBuilder 五感数据打通** → **已修复**（MaturePanel :32-48 `FormReference` 组件，按 sense_type 显示准备期描述）

**伦理提示：** 安全红线缺失是本次审计中**唯一涉及用户健康风险**的发现，建议优先级高于所有功能性缺陷。

**审计进度：** 批次 5/6 完成

---

## 批次 6：跨模块基础设施 — StatsPanel / useStats / MilestoneList / 新增功能

**审计文件：**
- `src/features/stats/StatsPanel.tsx` (29 行)
- `src/hooks/useStats.ts` (57 行)
- `src/features/journal/MilestoneList.tsx` (76 行)
- `src/db/database.ts:253-290`（统计查询实现）
- 本次新增：`MilestoneCelebrate.tsx` / `useMilestoneStore.ts` / `TimelineLayout.tsx`(虚拟滚动)

### 6.1 统计查询层（database.ts）— 时区口径

对四个统计查询逐一核验，发现**与批次 4 相反的结论**：

| 函数 | 时区处理 | 判定 |
|------|---------|------|
| `getResonanceEntries` (`:63`) | `date('now','localtime',...)` | ✅ 正确 |
| `created_at` 默认值 (`schema.ts:89`) | `datetime('now','localtime')` | ✅ 正确 |
| **`getDailyDurations`** (`:268`) | `date('now','-N days')` — **缺 localtime** | 🔴 **BUG** |
| **`getConsecutiveDays`** (`:281`) | `new Date().toISOString()` — **UTC** | 🔴 **BUG** |

#### 可靠性
| 严重度 | 发现 |
|--------|------|
| 🔴 | **`getDailyDurations` 时区不一致**（`:268`）。`created_at` 按 localtime 存储，但筛选条件用 UTC 的 `date('now',...)`。东八区用户在**本地时间 00:00–08:00** 之间，UTC 仍是前一天，导致「近 7 天」窗口边界错位一天。凌晨专注的记录可能落在错误的日期分组。 |
| 🔴 | **`getConsecutiveDays` 用 UTC 判定「今天」**（`:281`）。`new Date().toISOString().slice(0,10)` 返回 UTC 日期，而记录按本地日期存储。东八区用户在本地 00:00–08:00 期间打开应用，「今天」被判定为昨天 → **连续天数计算错误，可能显示断签**。这直接影响用户的坚持感知，是体验层面的严重问题。 |

> **修正批次 4 的结论：** 批次 4 核验的是 resonance 链路（正确），但统计链路存在时区缺陷。项目的时区处理**并非全局一致**。

#### 实用性
| 严重度 | 发现 |
|--------|------|
| 🟡 | `getTotalDuration` (`:253-257`) 统计**所有** `duration_seconds > 0` 的条目，包括 SwitchingLog 写入的换位练习时长（批次 5 指出）。「累计专注时长」口径被稀释，进而**影响里程碑 10/50/100h 的触发时机**。 |

---

### 6.2 useStats

#### 可靠性
| 严重度 | 发现 |
|--------|------|
| 🟡 | **catch 后仍设 `loading: false`**（`:47-51`）但不设错误态。查询失败时 StatsPanel 会渲染全零数据，用户看到「0 小时 0 分钟」而非错误提示——**将故障伪装成「你还没有记录」**。 |
| 🟡 | 无并发防护。快速多次 `refresh()`（如连续完成多个番茄钟）可能产生竞态，后发起的请求先返回时数据被旧结果覆盖。 |
| 🟢 | `Promise.all` 并行五查询（`:32-38`），性能处理得当。 |

---

### 6.3 MilestoneList 与里程碑双轨制

#### 实用性
| 严重度 | 发现 |
|--------|------|
| 🔴 | **里程碑存在两套互不相通的系统**。<br>① 手动里程碑 → `milestones` 表（`MilestoneList.tsx:26`）<br>② 自动时长里程碑 → `localStorage` 的 `milestones-celebrated`（`useMilestoneStore.ts`，本次新增）<br>用户在 10 小时触发庆祝动画后，**MilestoneList 中不会出现任何记录**。庆祝完即消失，没有留下痕迹。两套系统共用「里程碑」概念却零交集，是**本次新功能引入的架构断层**。 |
| 🔴 | **`localStorage` 存储里程碑状态与项目「本地 SQLite 单一数据源」原则冲突**。清除浏览器数据 / 更换设备 / WebView 缓存清理 → 已达成的里程碑丢失，会**重复触发庆祝**。而 `milestones` 表就在旁边，是现成的正确存储位置。 |
| 🟡 | 里程碑**无法删除**，误建条目永久保留。 |
| 🟡 | schema 中 `milestones.notes` 字段存在，创建时硬编码传空字符串（`:26`），UI 无入口。同 traits 的 `description` 字段闲置问题。 |

#### 可靠性
| 严重度 | 发现 |
|--------|------|
| 🟡 | `getMilestones()` 无参调用（`:18`）取全部里程碑，但创建时绑定 `activeStageId`（`:26`）。读写口径不对称，同 AutonomyLog 问题。 |
| 🟢 | `handleCreate` try/catch/finally + saving 锁完整。 |

#### 直觉性
| 严重度 | 发现 |
|--------|------|
| 🟡 | 每条里程碑都显示 `✓` 图标（`:53`），暗示「已完成」状态，但实际上创建即完成，无未完成态。设计规格提到「新建 + 标记完成」，当前只有新建。 |

#### 符合 tulpa 构建原理
| 严重度 | 发现 |
|--------|------|
| 🟢 | 手动里程碑符合社区实践——真正的进展节点（第一次听到声音、第一次自主回应）由用户判定，不可自动检测。 |
| 🟡 | 与 P6 张力：自动时长里程碑（10/50/100h）**奖励的是投入时长而非实际进展**。社区共识中，时长本身不是成就（有人 20 小时听到声音，有人 200 小时仍在努力），而庆祝动画会强化「时长=进步」的错误心智模型。 |

---

### 6.4 本次新增功能审计

#### MilestoneCelebrate（庆祝动画）
| 维度 | 严重度 | 发现 |
|------|--------|------|
| 可靠性 | 🟡 | `useEffect` 依赖数组仅 `[level]`（`:_`），但闭包捕获了 `onClose`。若父组件重渲染传入新 `onClose`，30 秒定时器仍持有旧引用。当前 `dismiss` 来自 Zustand 引用稳定，无实际问题，但属脆弱依赖。 |
| 便利性 | 🟡 | 30 秒自动关闭 + 点击关闭，但**无键盘 Esc 关闭**，全屏遮罩对键盘用户不友好。 |
| 直觉性 | 🟢 | 三级文案与视觉梯度设计得当，进度圆点清晰。 |
| tulpa 原理 | 🟡 | 见 6.3：庆祝时长里程碑强化了「时长=进步」的心智模型，与社区共识存在张力。文案「Ta 的回应越来越清晰」是**对用户实际进展的假设**，若用户实际卡在瓶颈期，此文案会产生反效果。 |

#### TimelineLayout 虚拟滚动
| 维度 | 严重度 | 发现 |
|------|--------|------|
| 可靠性 | 🔴 | **`loadAllEntries()` 无上限**。全量加载所有 entries 到内存。当前记录量下无碍，但设计规格的虚拟滚动初衷正是应对 >500 条场景——**用无限制全量加载来解决大数据量问题，方向自相矛盾**。10000 条记录时首屏加载会明显卡顿。应改为「大页量 + 滚动续载」。 |
| 可靠性 | 🟡 | `measureElement` 每次渲染创建新回调（`ref={(node) => virtualizer.measureElement(node)}`），可能导致额外的测量开销。 |
| 便利性 | 🟡 | 移除「加载更多」按钮后，**用户失去了对加载量的感知**。无总数显示，不知道自己有多少条记录。 |
| 直觉性 | 🟡 | 条目进场动画（`animate-[pageEnter]`）在虚拟滚动下**每次滚入都会重播**，快速滚动时视觉噪音明显。原分页方案下每条只播一次。 |

---

### 6.5 StatsPanel

#### 符合 tulpa 构建原理
| 严重度 | 发现 |
|--------|------|
| 🟢 | **文案是全项目对抗 P6 的最佳实践**：「不追赶数字，只看见每一次认真相伴」（`:18`）、「无需每天完美，保持自己的节奏」（`:22`）、「每一个有颜色的格子，都是一次靠近」（`:24`）。在一个纯数据面板中主动消解数字焦虑，与研究建议的方向一致。 |
| 🟡 | 但**文案与结构自相矛盾**：口号说「不追赶数字」，界面主体是巨大的累计时长数字 + 连续天数 + 三张图表。若真要落实文案意图，应弱化实时数字、强化周聚合回顾（P7 建议）。 |

#### 直觉性
| 严重度 | 发现 |
|--------|------|
| 🟡 | 近 7 天柱状图使用**相对高度**（`:22`，`item.total / max * 100`）。仅有一天有记录时，该柱显示满格，视觉上等同于「表现最好」，与 ResonanceTracker 已修复的固定比例做法不一致。 |

---

### 批次 6 汇总

| 维度 | 评价 |
|------|------|
| 实用性 | 🔴 里程碑双轨制割裂；localStorage 存储违背单一数据源 |
| 便利性 | ⚠️ 虚拟滚动移除了记录总量感知；庆祝弹窗无 Esc |
| 可靠性 | 🔴 两处 UTC/localtime 时区缺陷影响连续天数与日趋势 |
| 直觉性 | ⚠️ 7 天图相对高度误导；进场动画重播 |
| tulpa 原理 | 🟢 StatsPanel 文案最佳 · 🟡 时长里程碑强化错误心智模型 |

**必须修复（🔴）清单：**
1. ✅ `database.ts:268` — `getDailyDurations` 补 `localtime` → **已修复**（:277-284 `date('now','localtime',...)`）
2. ✅ `database.ts:281` — `getConsecutiveDays` 改用本地日期 → **已修复**（:287-304 本地 `getFullYear()/getMonth()/getDate()`）
3. ✅ `useMilestoneStore.ts` — 里程碑状态迁移至 `milestones` 表 → **已修复**（:26-36 `createMilestone` 写入 DB）
4. ✅ **自动里程碑达成后写入 `milestones` 表** → **已修复**（与手动里程碑合流）
5. ✅ `TimelineLayout.tsx` — `loadAllEntries` 加上限或改为大页量续载 → **已修复**（:11 `PAGE_SIZE=200` + 滚动分页加载 + "已加载 X / 共 Y 条" 指示器）

**审计进度：** 6/6 全部完成 ✅

---

# 总结

## 缺陷统计

| 批次 | 范围 | 🔴 必须修复 | 状态 | 🟡 建议修改 | 状态 |
|------|------|:---:|:---:|:---:|:---:|
| 1 | 准备期核心 | 5 | ✅ 已修复 | 8 | ✅ 6 · 🟡 2 |
| 2 | 准备期续 | 3 | ✅ 已修复 | 10 | ✅ 7 · 🟡 3 |
| 3 | 创建期 | 8 | ✅ 已修复 | 9 | ✅ 7 · 🟡 2 |
| 4 | 发展期 | 2 | ✅ 已修复 | 13 | ✅ 8 · 🟡 5 |
| 5 | 成熟期 | 5 | ✅ 已修复 | 12 | ✅ 8 · 🟡 4 |
| 6 | 跨模块 | 5 | ✅ 已修复 | 14 | ✅ 4 · 🟡 10 |
| **合计** | | **28** | **✅ 28/28** | **66** | **✅ ~40/66** |

## 五维总评

| 维度 | 评级 | 核心结论 |
|------|:---:|---------|
| **实用性** | ⚠️ 中 | 功能覆盖完整，但**蓝图数据全面孤岛化**——特质/形态/心象定义后不在任何实践环节复现。「回应迹象」有统计无录入。 |
| **便利性** | ⚠️ 中 | **草稿保护全面缺失**（速记/Wonderland/自主性日志），是最高频数据丢失风险。长页面滚动负担重。 |
| **可靠性** | ⚠️ 中 | 多数组件工程质量良好（CommitmentConfirm / SwitchingLog 可作范本），但存在 **`/T` 解析吞字符**、**两处时区缺陷**、**共振去重反向**三个真实数据错误。 |
| **直觉性** | ✅ 中上 | **文案是本项目最大优势**，AutonomyLog / WonderlandEditor 的引导语可作行业范本。主要问题是隐藏交互与无语义刻度（LEVEL 01-10）。 |
| **tulpa 原理** | 🔴 **最弱** | 见下。 |

## tulpa 构建原理符合度（最关键结论）

**做对的：**
- 🟢 **拒绝聊天界面、采用统一速记**——精准解决了实践者无法模拟双向对话的根本矛盾，同类工具中罕见
- 🟢 **AutonomyLog 是对抗怀疑（P4）的最有效设计**，「哪怕很小」的引导精确命中新手痛点
- 🟢 **Wonderland 版本化模型**天然支持 deviation，是正确范式
- 🟢 **StatsPanel 文案主动消解数字焦虑**，方向与临床建议一致

**系统性偏离：**

| # | 问题 | 违反 | 影响 | 状态 |
|---|------|------|------|------|
| 1 | **passive forcing / narration 完全缺席** | P1 | 社区公认价值最高的实践，在应用中无任何入口。 | ✅ **已修复** — 新增 QuickNarration 全局随手记入口 + PREP_SESSION_TYPES 增加 narration |
| 2 | **量化设计鼓励过度规格化** | P2 | 特质权重 1-10、形态五感无限条目、计数徽章奖励堆量。 | 🟡 **部分修复** — 新增预期管理卡片引导"基础轮廓已足够"；但权重数值 UI 仍存在 |
| 3 | **deviation 无处安放** | P3 | 全应用无任何位置可记录「Ta 和我最初设想的不一样了」。 | ✅ **已修复** — 新增 EvolutionLog 组件 + deviations 表，接入特质和形态 |
| 4 | **imposition 线性分级不成立** | P9 | `LEVEL 01-10` 在实践中无对应语义。 | ✅ **已修复** — 改为四级质变阶段（存在感/闪现/稳定/不透明） |
| 5 | **指标追踪密度过高** | P6 | 每日共振评分 + 四个实时计数 + 时长里程碑。 | 🟡 **待产品决策** — 每日评分频率需降频或改为可选 |
| 6 | **安全红线零提及** | P8 | 无任何「持续性 DPDR / 失控幻听应停止并求助」的提示。 | ✅ **已修复** — PossessionLog 加入安全警告文案 |
| 7 | **不使用 tulpa 的名字** | — | DialogueDisplay 硬编码 `Tulpa` 标签。 | ✅ **已修复** — 新增 useProfileStore 持久化名字，DialogueDisplay 动态读取 |

## 优先级建议

**P0 — 用户健康与数据正确性** ✅ 全部修复
1. ✅ 补充安全边界提示（P8 伦理缺口）
2. ✅ 修复 `/T` 解析吞字符（数据静默污染）
3. ✅ 修复两处时区缺陷（连续天数错误）
4. ✅ 修复共振每日去重方向

**P1 — 核心体验** ✅ 全部修复
5. ✅ 全面草稿保护（速记 / Wonderland / 自主性）
6. ✅ 「回应迹象」录入功能
7. ✅ `CreationPanel` 移除硬编码占位、统计改 DB 聚合
8. ✅ 里程碑双轨制合流，弃用 localStorage

**P2 — 原理性重构** ✅ 全部修复
9. ✅ 引入 passive forcing / narration 入口
10. ✅ imposition 改为质变阶段模型
11. ✅ 建立 deviation 记录路径（复用 Wonderland 版本化范式）
12. ✅ 蓝图数据（特质/形态）在实践环节复现
13. 🟡 **重新评估 ResonanceTracker 的每日评分频率** — 待产品决策（见下方）

---

> 审计完成 2026-07-26 · 6 批次 · 覆盖 20 个源文件 + DB 层
> 研究基线：tulpa.info 社区指南、r/Tulpas、临床文献（日记疗效 / 自我监测风险 / 解离风险）
> 工具：全程手动审计，tavily 仅用于获取 tulpa 构建原理基准知识
> **修复验证：2026-07-28 · 全部 🔴 已修复 · TypeScript 编译零错误**

---

# 产品方向待决策项（2026-07-28 新增）

以下项并非缺陷，而是审计报告中指出需要产品方向决策的设计选择。每项附社区原理依据和可选方案。

## 1. ResonanceTracker 每日评分频率（P6 张力）

**现状：** 情感共振追踪器允许每日记录 1-5 分的情感连接强度。

**审计发现：** 临床研究警示 NJRE/OCD 倾向人群中，逐日自我监测会放大「今天分数低是不是我做错了」的痛苦。建议**小指标集 + 周聚合回顾**。

**可选方案：**
- A. 保持每日但改为可选展示（默认隐藏，用户主动展开）
- B. 降频为每周一次记录（UI 引导"本周的整体感受"）
- C. 保留每日入口但移除数字评分，改为情绪词/emoji 表达

## 2. 时长里程碑"时长=进步"心智模型（P6 张力）

**现状：** 累计专注达到 10/50/100 小时触发庆祝动画，文案"Ta 的回应越来越清晰"。

**审计发现：** 社区共识中，时长本身不是成就（有人 20 小时听到声音，有人 200 小时仍在努力）。庆祝时长可能强化错误心智模型，且文案对用户实际进展的假设可能产生反效果。

**可选方案：**
- A. 保留庆祝但修改文案为"每一小时的陪伴都算数"（去除进展假设）
- B. 将自动里程碑改为手动触发（由用户决定何时庆祝）
- C. 取消时长里程碑，仅保留手动里程碑

## 3. 特质权重 1-10 数值 UI（P2 over-specification）

**现状：** 创建特质时用 1-10 滑块表示"重要程度"，编辑时用 +/− 按钮调整。

**审计发现：** 社区共识认为特质应是柔性起点，会自然演化。精确数值排序暗示特质是可固定规格化的。但此设计也提供了快速浏览的便利性。

**可选方案：**
- A. 保留数值但改为描述性档位（如"核心 / 重要 / 一般"三档）
- B. 完全移除权重，仅保留特质名称 + 描述
- C. 保留当前设计（已加入"会自然演化"的引导文案）

## 下一步

以上产品方向问题需要通过头脑风暴确定设计方案。
